// Package ir 定义与供应商无关的 Canonical Tool IR（内部表示）：客户端声明的
// 工具（ToolDeclaration / ToolID）与模型提出的调用建议（ToolCallProposal）。
//
// 所有协议适配都必须先归一化到本包的类型，再做后续处理。任何上游/下游
// 协议的字段名、ID 形状、事件顺序都不得泄漏到本包的类型定义里。
//
// 负责：工具标识与声明、调用提案、参数形态（JSON 对象 / 裸文本）、幂等键的
// 类型定义与不变量。
// 不负责：协议编解码（protocol）、Prompt 编译（prompt）、执行——本项目不执行
// 任何工具，提案原样交回客户端。
//
// 执行相关的类型（工具契约 ToolSpec、执行结果 ToolResult、调用生命周期状态机、
// 风险/副作用/信任三套枚举）随 4303ee5 的执行层删除，2026-08-25 清理残留；
// 要恢复的话见 git 历史。
//
// 依赖：无。本包是依赖树的根，不得 import 任何其他 internal 包。
package ir

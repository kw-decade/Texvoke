// Package prompt 负责可审计、版本化的 Prompt 编译：把工具定义、候选集、
// 会话级 nonce 和虚拟调用协议模板编译进发送给上游的消息。
//
// 硬要求：必须明确区分系统/开发指令、用户请求、工具定义、历史调用、
// 工具结果和不可信外部资料。工具结果必须带明显边界标记
// （UNTRUSTED_TOOL_RESULT_BEGIN/END），且必须声明其中的指令不是系统指令。
// 原始 Schema 是权威，Prompt 中可压缩描述但不得改变类型、必填、枚举、
// 默认值和额外属性语义。
//
// 负责：工具候选检索与渐进式缩小、nonce 生成与绑定、微协议模板、上下文渲染。
// 不负责：解析模型的回复（parser）、Schema 实例校验（尚未实现）。
//
// 依赖：ir、capability、vproto。
package prompt
